DATABASE_URL=mysql+pymysql://user:password@localhost:3306/regnskap
SECRET_KEY=change-this-to-a-random-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# WebAuthn / Passkey settings
RP_ID=localhost
RP_NAME=Regnskap

# SMTP settings for password reset emails
SMTP_HOST=smtp.altibox.no
SMTP_PORT=465
SMTP_USER=flextime@altiboxmail.no
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=flextime@altiboxmail.no
